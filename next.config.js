/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost'], // Adicione o domínio onde você planeja armazenar as imagens
      },
}
module.exports = nextConfig
